<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /sync/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-l
  RewriteRule . /sync/index.html [L]
</IfModule>